# 前端环境变量模板（复制为 .env.local 并填值；.env.local 已被 git 忽略）

# 后端 API 地址（本地开发默认；生产指向服务器）
VITE_API_URL=http://localhost:3001

# 版本检查源（仅提示式更新检查；指向 releases/latest API，仓库定稿后替换）
# VITE_UPDATE_CHECK_URL=https://api.github.com/repos/<OWNER>/<REPO>/releases/latest
